# 复制为 .env 并填入真实值，切勿提交 .env 到 git！
# 使用方式: 复制此文件为 .env，config.py 会自动读取加载

GATE_API_KEY=
GATE_API_SECRET=

# 保护 /api/start /api/stop /api/emergency_cancel /api/update_params 等控制接口
# 留空则不启用鉴权（仅建议在 127.0.0.1 本机环境下这样做）
WEB_API_TOKEN=

# 默认是模拟盘域名，切换实盘前务必确认这里也一并改掉
# GATE_REST_HOST=https://api.gateio.ws
